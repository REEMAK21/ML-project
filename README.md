
# Data EDA + ML

This project is part of a **milestone in the AI Professionals Bootcamp**. It focuses on **Machine Learning ML**,
**pipelines**, data manipulation with **pandas**, visualization, and developing analytical skills including storytelling from data insights.

---

## Prerequisites

- Python 3.11+  
- Git installed  
- (Recommended) Virtual environment support
- uv
  
> **Note:** All commands assume you are in the project root directory (`week3-ml-baseline-system`) and the `.venv` is active.

Open Shipping_Crisis_Template(1).ipynb

> **Note:** Before running the cells, make sure to select the kernel corresponding to your .venv virtual environment. This ensures all dependencies are available.
